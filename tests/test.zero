(http_server 8080
  (import "github.com/google/uuid")
  (struct RequestData (name string))
  (route "/api" (lambda (req)
    (let (mykey (env "API_KEY"))
      (let (data (parse_json RequestData req.body))
        (res_json 200 data)
      )
    )
  ))
)
