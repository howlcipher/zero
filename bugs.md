# 🐛 Bug Backlog

This document is the authoritative, ranked backlog for known flaws, bugs, and broken items specifically for the Zero transpiler project. It mirrors the structure of `improvements.md` and follows the same Working Protocol.

## Ranked Backlog (best ROI first)

Pending bugs carry the same diminishing-returns score defined in `improvements.md` (Score = Value × Decay ÷ Effort). Bugs rarely decay, so Decay is normally 1.0.

| # | Bug | Status | Score (V×D÷E) | Claude model | Gemini model | ROI rationale |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | [Lexer panics on EOF during unterminated string](#1-lexer-panics-on-eof-during-unterminated-string) | Done | 4.0 (4×1÷1) | Haiku 3 | Gemini 1.5 Flash | The lexer reports the error via `reportError`, but an explicit bounds check prevents potential runtime panics during deep parsing. |
| 2 | [Python Orchestrator timeout with heavy models](#2-python-orchestrator-timeout-with-heavy-models) | Done | 3.0 (3×1÷1) | Haiku 3 | Gemini 1.5 Flash | Added explicit UX warnings to console so users don't assume the script has frozen when loading heavy models in Outlines. |
| 3 | [Variable Shadowing in Go Generation](#3-variable-shadowing-in-go-generation) | Done | 4.0 (4×1÷1) | Sonnet 3.5 | Gemini 1.5 Pro | `let` expressions without inner `{}` brackets risk leaking scopes or redeclaring variables, causing Go compilation to fail on nested conditionals. |
| 4 | [Outlines EBNF Compilation Memory Limit](#4-outlines-ebnf-compilation-memory-limit) | Done | 2.5 (5×1÷2) | Sonnet 3.5 | Gemini 1.5 Pro | Optimized the CFG to only enforce generic S-expression balanced parentheses, avoiding OOM on massive state machines while deferring semantic validation to the Go transpiler. |
| 5 | [AST Deep Nesting Stack Limits](#5-ast-deep-nesting-stack-limits) | Done | 2.0 (4×1÷2) | Sonnet 3.5 | Gemini 1.5 Pro | Added an explicit depth parameter and limit of 1000 to `generateStatement` to fail gracefully with a JSON error instead of crashing the Go call stack. |
| 6 | [Include Paths Relative to File](#6-include-paths-relative-to-file) | Done (2026-07-22) | 6.0 (6×1÷1) | Sonnet 3.5 | Gemini 1.5 Pro | `(include "file.zero")` uses current working directory rather than resolving paths relative to the file doing the inclusion. |
| 7 | [defun Typing Rigidness](#7-defun-typing-rigidness) | Done (2026-07-22) | 1.75 (7×1÷4) | Sonnet 3.5 | Gemini 1.5 Pro | All `defun` arguments strictly compile to Go `string` types, breaking if we try to pass an `*http.Request` or `int` to a function. |
| 8 | [try_let Error Interception Rigidness](#8-try_let-error-interception-rigidness) | Done (2026-07-23) | 1.66 (5×1÷3) | Sonnet 3.5 | Gemini 1.5 Pro | `try_let` is currently hardcoded to only support `parse_json` as the error-returning function. Needs generalization. |
| 11 | [No Runtime Source Mapping](#11-no-runtime-source-mapping) | Done (2026-07-23) | 6.0 (6×1÷1) | Haiku 3 | Gemini 1.5 Flash | Go panics at runtime do not map back to `.zero` files. Need Go `//line` directives. |
| 12 | [Lexer cannot tokenize `!=`](#12-lexer-cannot-tokenize-) | Done (2026-07-23) | 8.0 (8×1÷1) | Haiku 3 | Gemini 1.5 Flash | The lexer's symbol character class omits `!`, so any script using `!=` fails to lex at all — including the README's own `test` block example. One-line fix. |
| 13 | [`return` in `defun` drops compound expressions](#13-return-in-defun-drops-compound-expressions) | Done (2026-07-23) | 3.5 (7×1÷2) | Sonnet 3.5 | Gemini 1.5 Pro | `(return (+ a b))` or `(return (call f x))` emits a bare `return` with no value, which fails Go compilation for any non-void `defun`. Only bare symbols/literals currently work. |
| 14 | [`(import)` duplicated/unused in `server_test.go`](#14-import-pkg-duplicatedunused-in-generated-server_testgo) | Done (2026-07-23) | 3.0 (6×1÷2) | Sonnet 3.5 | Gemini 1.5 Pro | Custom imports get blindly copied into both `server.go` and `server_test.go`, causing duplicate-import or unused-import compile failures once a script combines `import`, `defun`, and `test` blocks. |
| 15 | [`test_include.zero` references stale `routes.zero` path](#15-test_includezero-references-stale-routeszero-path) | Done (2026-07-23) | 3.0 (6×1÷2) | Haiku 3 | Gemini 1.5 Flash | Improvement #42's file-structure cleanup moved `routes.zero` into `examples/`, but `tests/test_include.zero` still does `(include "routes.zero")`, which resolves relative to the CWD — the file now fails with `no such file or directory`. |
| 16 | [`if` requires an else branch — no single-branch `if`](#16-if-requires-an-else-branch--no-single-branch-if) | Done (2026-07-23) | 4.5 (9×1÷2) | Sonnet 3.5 | Gemini 1.5 Pro | `generateStatement`'s `"if"` case hard-requires exactly 4 children (`if cond then else`) and errors otherwise; there is no way to write a guard clause / early-return style `if` with no else, a pattern nearly all AI-generated code reaches for. |
| 21 | [`env` emits an unquoted Go identifier instead of a string literal](#21-env-emits-an-unquoted-go-identifier-instead-of-a-string-literal) | Done (2026-07-23) | 8.0 (8×1÷1) | Haiku 3 | Gemini 1.5 Flash | A leftover dead-code line unconditionally overwrites the correctly-quoted `os.Getenv(%q)` output with an unquoted `os.Getenv(%s)`, so every `(env "KEY")` call generates `os.Getenv(KEY)` — a reference to an undefined Go identifier — instead of `os.Getenv("KEY")`. Breaks every use of a documented, security-critical primitive. One-line fix. |
| 10 | [String Escaping Limitations](#10-string-escaping-limitations) | Done | 5.0 (5×1÷1) | Haiku 3 | Gemini 1.5 Flash | Lexer breaks on unicode escapes and escaped single quotes, common in LLM outputs. |
| 9 | [Depth Limit Crash via `let` Chaining](#9-depth-limit-crash-via-let-chaining) | Done (2026-07-23) | 4.0 (4×1÷1) | Haiku 3 | Gemini 1.5 Flash | Long sequential scripts with variable assignments crash transpiler with `AST too deep`. Scope needs flattening. |
| 23 | [Lexer has no comment syntax](#23-lexer-has-no-comment-syntax--prefixed-comments-in-documented-examples-fail-to-lex) | Done (2026-07-23) | 7.0 (7×1÷1) | Haiku 3 | Gemini 1.5 Flash | No `;` line-comment handling anywhere in the lexer; broke two of README's own documented examples as written. Doc examples fixed directly; lexer fix itself still pending. |
| 24 | [No way to write a void `defun`](#24-no-way-to-write-a-defun-with-no-return-value-void-function) | Done (2026-07-23) | 4.0 (8×1÷2) | Sonnet 5 | — | Every `defun` defaults to a Go `string` return type with no way to opt out; a pure side-effect function (e.g. a `print`-only helper) fails `go build` with `missing return`, and the natural `(type_hint return "void")` escape hatch fails with `undefined: void`. |
| 25 | [`let` with more than one body statement infinite-recurses instead of erroring](#25-let-with-more-than-one-body-statement-infinite-recurses-instead-of-erroring) | Done (2026-07-24) | 6.0 (6×1÷1) | Sonnet 5 | — | `(let (var val) stmt1 stmt2)` — a natural mistake, since nothing in the grammar signals that `let`'s body is a single form — silently infinite-recurses on itself until the depth-1000 guard trips, producing a baffling `"AST too deep"` error instead of a clear arity message. Reproduces identically in both the Go and JS backends; found while writing a README example for the (previously undocumented) JS/`web_app` backend. |
| 26 | [`list`/`dict` literals only work as a `let` binding's direct value, everywhere else fails with a generic error](#26-listdict-literals-only-work-as-a-let-bindings-direct-value-everywhere-else-fails-with-a-generic-error) | Done (2026-07-24) | 2.33 (7×1÷3) | Sonnet 5 | — | `(print (list "a" "b"))`, `(return (list "a" "b"))`, and any other non-`let` expression position for a `list`/`dict` literal fails with `"Unknown statement: list"` in both the Go and JS backends — the construction logic only exists inside `let`'s hand-rolled value-position special case. The Phase 1 interpreter (#49) has no such restriction. Found while implementing improvement #60. |
| 27 | [Go backend's `list`/`dict` literal construction silently drops compound-expression elements/values to empty](#27-go-backends-listdict-literal-construction-silently-drops-compound-expression-elementsvalues-to-empty) | Done (2026-07-24) | 0.75 (6×0.25÷2) | Sonnet 5 | — | Same "leaf `.Value` read instead of recursing" root-cause theme as bugs #13 and #20 (both Done) — decay 0.25 (2 prior same-theme ships). Small, well-scoped fix mirroring those two exact precedents. |
| 28 | [`dict` literal keys silently drop to empty when the key is a compound expression, producing invalid Go syntax](#28-dict-literal-keys-silently-drop-to-empty-when-the-key-is-a-compound-expression-producing-invalid-go-syntax) | ⚠️ below floor | 0.375 (6×0.125÷2) | Sonnet 5 | — | Same "leaf `.Value` read instead of recursing" theme as bugs #13/#20/#27 (3 prior same-theme ships) — decay 0.125. Deliberately left out of bug #27's scope (which covered elements/values, not keys). |
| 17 | [No string-to-number parsing primitive](#17-no-string-to-number-parsing-primitive) | Done (2026-07-23) | 3.0 (6×1÷2) | Sonnet 5 | — | Zero has `read_file`/`str_split`/`+`/`-`/`*`/`/` but no lightweight `to_int`/`to_float`/`parse_number` node, so a numeric-literal string read from a file or arg can only be turned into a real number via the heavyweight, nondeterministic, network-dependent `fuzzy_cast` (LLM round-trip) — there is no deterministic path at all. |
| 22 | [`tests/test.zero` depends on an unvendored external module, breaking offline builds](#22-teststestzero-depends-on-an-unvendored-external-module-breaking-offline-builds) | Done (2026-07-23) | 3.0 (3×1÷1) | Haiku 3 | Gemini 1.5 Flash | `tests/test.zero` does `(import "github.com/google/uuid")`, but `go.mod` has no `require` for it and there is no `go.sum`, so `go build` fails offline with "no required module provides package" — noted as a recurring "pre-existing, unrelated failure" in bugs #12/#13/#14's verification notes without ever being tracked as its own item. Re-scored 2026-07-23: network access to proxy.golang.org now confirmed working in this environment, so `go get`+commit is a concrete Effort-1 fix rather than a blocked/hypothetical one. |
| 18 | [`if` condition rejects compound/logical expressions, and silently mis-compiles them](#18-if-condition-rejects-compoundlogical-expressions-and-silently-mis-compiles-them) | Done (2026-07-24) | 2.67 (8×1÷3) | Sonnet 5 | — | `if`'s condition parser only accepts a flat `(op literal literal)` comparison; `and`/`or` conditions error out, and nested arithmetic on either side silently emits broken Go (`if  > 4 {`) instead of erroring. Discovered while regression-testing the bug #16 fix. |
| 19 | [`//line` directive corrupts any compound expression nested inside another compound expression](#19-line-directive-corrupts-any-compound-expression-nested-inside-another-compound-expression) | Done (2026-07-23) | 2.67 (8×1÷3) | Sonnet 5 | — | `generateStatement` prefixes a `//line file:N` comment for a fixed set of heads (including `call`); any code path that embeds a recursive `generateStatement` result *inside* a larger expression string (binary ops, `assert_semantic`, `return`, `cli_args` index, etc.) gets that comment spliced mid-expression, corrupting the emitted Go whenever the nested node's head is one of the wrapped ones. Discovered while fixing bug #13. |
| 20 | [`call` arguments that are compound expressions are silently dropped to empty](#20-call-arguments-that-are-compound-expressions-are-silently-dropped-to-empty) | Done (2026-07-23) | 1.33 (8×0.5÷3) | Sonnet 5 | — | `call`'s argument loop reads `argNode.Value` directly instead of recursing via `generateStatement`/`generateStatementRaw`, so any non-string, non-bare-symbol argument (nested `call`, arithmetic, etc.) silently becomes an empty string — e.g. `(call f (call g 1))` emits `f()` with `g(1)` dropped entirely, no error. Discovered while regression-testing bug #14. Re-scored 2026-07-23: decay 0.5 applied — same "leaf `.Value` read instead of recursing into a compound expression" root-cause theme as bug #13 (Done), which fixed the identical defect class for `return`'s value position. |
## Details

### 1. Lexer panics on EOF during unterminated string
In `zero.go`, the string lexer currently scans for the next quote. If EOF is hit, it correctly calls `reportError`, but if the file is truly truncated, we should ensure no other routines attempt to consume past the array bounds. The code is mostly safe but needs explicit unit tests to guarantee it never panics outside of the controlled JSON error output.

### 2. Python Orchestrator timeout with heavy models
When running `orchestrator.py` against a local Ollama instance with a large model (e.g. 70B parameters), the structured generation engine in `outlines` might hang for a long time compiling the regex/grammar. We need to add logging or a loading indicator so the user knows the compilation/generation hasn't silently frozen.

### 6. Include Paths Relative to File
* **Description:** `(include "file.zero")` uses `os.ReadFile(filename)`.
* **Why:** If the zero compiler is run from a different directory, it will fail to find relative included files.
* **Impact:** 6/10 (High).

### 7. defun Typing Rigidness
* **Description:** `defun` maps all arguments to strings in Go.
* **Why:** We cannot cleanly pass `req` to helper functions without using strings.
* **Impact:** 7/10 (High).

### 8. try_let Error Interception Rigidness
* **Description:** `try_let` only intercepts `parse_json` right now.
* **Why:** Needs to generalize to any function returning `(value, error)`.
* **Impact:** 5/10 (Medium).

### 9. Depth Limit Crash via `let` Chaining
* **Description:** Every `let` block increments the AST depth (`depth+1` at line 644). If an AI generates a long sequential script with 1,000 variable assignments, the transpiler crashes with `AST too deep`.
* **Why:** Variable scope needs to be flattened instead of infinitely nesting in AST.
* **Impact:** 8/10 (High).

### 10. String Escaping Limitations
* **Description:** The lexer only supports `\n` and `\t`. It breaks on unicode escapes (`\uXXXX`) and escaped single quotes (`\'`).
* **Why:** LLMs frequently output these escapes, especially in JSON strings.
* **Impact:** 7/10 (High).

### 11. No Runtime Source Mapping
* **Description:** Runtime panics point to `main.go`, not the `.zero` file.
* **Why:** The transpiler should inject Go `//line filename.zero:line` directives so runtime panics map back to the AI's code.
* **Impact:** 6/10 (Medium).

### 12. Lexer cannot tokenize `!=`
* **Description:** `lexer()` in `zero.go` accepts `=`, `<`, `>`, `+`, `-`, `*`, `.`, `/`, `_` as symbol start/continuation characters (~line 140), but not `!`. Any `.zero` source containing `!=` fails at the lex stage with `Unexpected character: !`, before the parser is ever reached — even though `generateStatement`'s `if`/`match` condition handling (lines ~830, ~930) explicitly checks for and supports an `"!="` operator string. The operator support is dead code today.
* **Why:** Not-equal comparisons are basic control flow. Discovered while verifying improvement #16 (Native Unit Test Blocks): the exact `(if (!= result 5) ...)` example added to the README's "Native Unit Test Blocks" section (and `docs/index.html`) does not lex, so the flagship new-feature example is broken as documented.
* **Impact:** 8/10 (High — silently breaks a documented, common comparison operator).
* **Repro:** `echo '(cli_app (if (!= 1 2) (print "ne")))' > /tmp/t.zero && ./zero /tmp/t.zero` → `{"reason":"Unexpected character: !","line":1,...}`.
* **Fix sketch:** add `ch == '!'` to both character-class checks at zero.go:140 and zero.go:142.
* **Done (2026-07-23):** Added `ch == '!'` / `l.peekChar() == '!'` to the two symbol character-class checks in `lexer()`. Verified `(!= 1 2)` now lexes and the generated Go compiles (`go build`) via `go run zero.go`. Applied directly (trivial, fully-specified two-token diff) after the autonomous `agy --mode accept-edits` delegation call was blocked by the session's permission classifier. Ran the full `tests/*.zero` suite through the transpiler afterward; all pass except `tests/test_include.zero`, which fails for an unrelated pre-existing reason — filed as bug #15.

### 13. `return` in `defun` drops compound expressions
* **Description:** In `generateStatement`, the `"return"` case (zero.go:597-606) does `fmt.Sprintf("return %s", valNode.Value)`. `Node.Value` is only populated for leaf tokens (SYMBOL/STRING/NUMBER) — for a compound expression node like `(+ a b)` or `(call f x)`, `Value` is empty, so the case silently emits a bare `return` with nothing after it. Every other statement/expression case recursively calls `generateStatement(valNode, reqVar, depth+1)`; `return` is the outlier.
* **Why:** Discovered via the same repro as bug #12 — even after fixing `!=` lexing, the README's `(defun add (a b) (return (+ a b)))` example fails to compile in the generated Go (`not enough return values`), because the return value is silently dropped. This affects any `defun` whose return value is not a bare variable or literal (arithmetic, `call`, string concatenation, etc.), a large fraction of realistic function bodies.
* **Why it went unnoticed:** the transpiler exits 0 and produces syntactically-valid-looking Go for simple `(return x)`/`(return "str")` cases; the failure only shows up as a downstream `go build` error on the generated code, which existing example `.zero` files apparently don't exercise.
* **Impact:** 7/10 (High — undermines improvement #6 Function Definitions for any non-trivial return value).
* **Repro:** `(cli_app (defun add (a b) (return (+ a b))) (print (call add 2 3)))` → generated `func add(a string, b string) string { return \n}`.
* **Fix sketch:** replace `valNode.Value` with `generateStatement(valNode, reqVar, depth+1)` in the non-STRING branch, mirroring how other cases (e.g. `call`, binary ops) already recurse.
* **Done (2026-07-23):** Delegated the literal fix-sketch diff to `gemini-3.1-pro-low` via `agy --mode accept-edits`; `git diff` confirmed a real, exactly-matching one-line change. However, verifying it against both named repro cases (`(return (+ a b))` and `(return (call f x))`) surfaced that the sketch's `generateStatement` call was itself wrong for the `call` case: `generateStatement` (unlike `generateStatementRaw`) injects a `//line` directive wrapper for certain heads (`call` included), which corrupts the output when embedded mid-expression inside the `return %s` `Sprintf` — it emitted `return //line ...\n\t\tadd(a, b)`, a bare `return` followed by a floating, uncompiled call, failing with `not enough return values`. Fixed directly by using `generateStatementRaw` instead of `generateStatement` for the recursive call, which has no such wrapping. Added `tests/test_return_compound.zero` covering both the arithmetic (`(+ a b)`) and nested-call (`(call add a b)`) cases; both transpile, `go build`, and run correctly (`add(2,3) == 5`, `add_via_call(4,5) == 9`). Full existing `tests/*.zero`/`examples/*.zero` suite re-run with no new regressions (pre-existing unrelated failures in `test_include.zero` (bug #15), `test_middleware.zero` (missing `TOKEN` env var), and `test.zero` (missing `uuid` Go module) reproduce identically on the pre-fix code). **New finding:** the same `//line`-in-sub-expression corruption exists more broadly — any compound expression recursing through `generateStatement` (not `generateStatementRaw`) whose head is in the wrapped-heads list (e.g. a `call` nested inside a `+`, like `(+ (call f) 1)`) produces the same corrupted output; filed as bug #19 since it's wider than just `return`.

### 14. `(import "pkg")` duplicated/unused in generated `server_test.go`
* **Description:** `generateCode` appends every `extraImports` entry into both `server.go`'s and `server_test.go`'s import blocks unconditionally (zero.go:488 and zero.go:540). Two problems: (a) if a custom import happens to name a package already in the hardcoded default import list (e.g. `(import "strings")`), the generated file declares it twice → Go "imported and not used" / redeclare compile error; (b) even for non-colliding packages, `server_test.go` has no `var _ = pkg.Symbol` suppression the way the default imports do, so a package used only by non-test code (routes/defuns) but not referenced inside any `(test ...)` body makes `server_test.go` fail with "imported and not used" as soon as any `test` block exists alongside any `import`.
* **Why:** Found while verifying improvement #16 (Native Unit Test Blocks) end-to-end with a `.zero` file that combines `(import ...)` with a `(defun ...)` and a `(test ...)` block — a realistic combination now that both features exist.
* **Impact:** 6/10 (Medium-High — breaks the common case of testing a `defun` that relies on an external package).
* **Repro:** `(cli_app (import "strings") (defun shout (s) (return (call strings.ToUpper s))) (test "d" (print (call shout "hi"))))` → `server.go` and `server_test.go` both declare `"strings"` twice in their import blocks.
* **Fix sketch:** dedupe `extraImports` against the hardcoded default list before appending; for `server_test.go`, either only include imports actually referenced within test bodies, or emit `var _ = ` blanks for extraImports the same way defaults are handled.
* **Done (2026-07-23):** Deduped `extraImports` against the hardcoded default package list (and against itself) at the point custom `(import "pkg")` nodes are collected (zero.go:319-329), so a colliding custom import like `(import "strings")` is silently dropped rather than duplicated. For `server_test.go`, filtered `extraImports` to only the packages whose derived local identifier (last path segment) is textually referenced (`"<pkg>."`) inside the assembled `testCode`, leaving `server.go` unconditionally including every deduped extra import as before. Delegated to Antigravity CLI (agy) / `gemini-3.1-pro-high`; diff verified real via `git diff` before trusting the summary. Verified against both sub-cases: (a) `(import "strings")` colliding with the default list no longer duplicates and both generated files build; (b) `(import "math/rand")` used only inside a `defun` (not referenced in any `test` body) is included in `server.go` and correctly omitted from `server_test.go`, and both build. Full `tests/*.zero` fixture suite re-run with no new regressions (only the pre-existing, unrelated bug #15 `test_include.zero` failure remains).

### 15. `test_include.zero` references stale `routes.zero` path
* **Description:** Improvement #42 (Clean up file structure) moved `routes.zero` from the repo root into `examples/`, but `tests/test_include.zero` still contains `(include "routes.zero")`. Since `include` resolves relative to the process's working directory (this is bug #6's underlying issue, previously fixed only for the file-doing-the-including's own directory, not for a moved target), running the test from the repo root now fails.
* **Why:** Found while regression-testing the `tests/*.zero` suite after fixing bug #12 (`!=` lexing) — it is unrelated to that fix and reproduces identically against the pre-#12 `zero.go`, so it's a leftover from the #42 file reorganization that nothing caught at the time.
* **Impact:** 6/10 (Medium-High — one of the repo's own test fixtures is currently broken).
* **Repro:** `go run zero.go tests/test_include.zero` → `{"reason":"Failed to read included file \"routes.zero\": open tests/routes.zero: no such file or directory",...}`.
* **Fix sketch:** either update `tests/test_include.zero`'s include path to `../examples/routes.zero`, or copy/move a `routes.zero` fixture into `tests/` alongside it — whichever keeps the test's intent (demonstrating same-directory relative includes) clearest.
* **Done (2026-07-23):** Copied `examples/routes.zero` into `tests/routes.zero`, keeping the test's original intent (demonstrating same-directory relative includes, matching bug #6's fix) rather than pointing across directories. Applied directly (trivial, fully-specified single-file copy) rather than delegating. Verified `go run zero.go tests/test_include.zero` now transpiles and builds cleanly (`go build`, `go vet` both pass). Re-ran every fixture under `tests/*.zero` and `examples/*.zero` through the transpiler: the only failures are `tests/routes.zero` and `examples/routes.zero` themselves (both are `module` fragments meant for `include`, not standalone roots — expected, not a regression) plus the pre-existing, unrelated `test_middleware.zero` (missing `TOKEN` env var) and `test.zero` (missing `uuid` Go module) failures noted in bug #12/#13's verification.

### 16. `if` requires an else branch — no single-branch `if`
* **Description:** In `generateStatement`, the `"if"` case (zero.go:818-821) does `if len(node.Children) != 4 { reportError("if expects (if cond then else)", ...) }`, so `(if cond then)` with no third body form always fails to parse — there is no way to write `(if cond then)` as a guard/early-return without an else branch.
* **Why:** Discovered while regression-testing the fix for bug #12: the README's own "Native Unit Test Blocks" flagship example (`(if (!= result 5) (print "Error: ..."))`, no else) does not parse at all, independent of and in addition to bugs #12 and #13 — even after both of those are fixed, this example still fails with `if expects (if cond then else)`. Single-branch guard clauses are an extremely common pattern in AI-generated code (e.g. validation checks, early logging), so this is a real gap, not just a doc mismatch.
* **Impact:** 9/10 (High — blocks a fundamental, universally-expected control-flow pattern).
* **Repro:** `(cli_app (if (!= 1 2) (print "ne")))` → `{"reason":"if expects (if cond then else)",...}`.
* **Fix sketch:** relax the length check to accept 3 or 4 children; when there are only 3 (`if cond then`), generate an empty `else {}` branch (or omit the `else` clause entirely in the emitted Go).
* **Done (2026-07-23):** `generateStatement`'s `"if"` case (zero.go) now accepts 3 or 4 children; a 3-child `(if cond then)` emits Go with no `else` clause at all (rather than an empty one), while the existing 4-child form is untouched. Regression test added at `tests/test_if_no_else.zero` (a guard-clause `if` plus a single-branch error check), transpiled and run successfully via `go run zero.go` + `go run server.go`; full existing `tests/*.zero` and `examples/*.zero` suite re-transpiled with no new errors. Note: the current README's "Native Unit Test Blocks" example (`README.md` lines 234-241) actually already has both branches (`if`/`else`) — this bug's "Why" quote of a no-else version was either a prior draft or paraphrase, not the file as it stands today; the general capability gap (no way to omit `else` at all) was still real and is what this fix closes.

### 17. No string-to-number parsing primitive
* **Description:** The full set of statement heads in `zero.go` includes `read_file`, `str_split`, `str_join`, `regex_match`, and the arithmetic operators `+ - * /`, but no `to_int`, `to_float`, `parse_number`, or similar node. Unquoted numeric literals compile straight to Go int/float literals, so `(+ x 1)` works fine when `x` was itself assigned a literal — but a string obtained at runtime (from `read_file`, `cli_args`, or `str_split` output) has no deterministic way to become a number. The only existing primitive that touches type coercion at all is `fuzzy_cast`, which round-trips through an LLM (`llm_generate` under the hood) and needs Ollama running — wildly disproportionate, nondeterministic, and slow for turning `"42"` into `42`.
* **Why:** Discovered while building improvement #44's cross-language write-cost benchmark: the original Task B ("read a file, sum a numeric column") could not be written in Zero at all without abusing `fuzzy_cast` per line, so the benchmark task had to be redesigned around string-only processing to keep the 6-language comparison fair. This is a real capability gap, not a benchmark artifact.
* **Impact:** 6/10 (Medium-High — blocks a very common pattern: reading numeric config/data from files or CLI args).
* **Repro:** no lex/parse error occurs; there is simply no node to reach for. `(let (n (call str_to_int "42")) (print (+ n 1)))` transpiles with no error (`call`'s codegen blindly emits any symbol as a bare Go identifier) but fails at `go build` with `undefined: str_to_int` — the corrected symptom as of the 2026-07-23 groom pass; the previously-written "`Unknown statement: str_to_int`" transpile-time error does not actually occur.
* **Fix sketch:** add a `to_int`/`to_float` node that emits Go's `strconv.Atoi`/`strconv.ParseFloat` inline (mirroring how `cli_args`'s index arg already uses `strconv.Atoi` internally at zero.go:1216), with a sensible zero-value-on-error default or paired `try_let` support so parse failures route through the existing error-handling primitive instead of panicking.
* **Groomed (2026-07-23):** re-verified against current zero.go; capability gap and impact unchanged, only the line reference and repro symptom above were corrected.
* **Related finding:** `read_file` (zero.go:987-992) returns Go's `os.ReadFile` result directly, typed `[]byte`, with no native primitive to convert it to `string` either — so even pure string processing (e.g. `str_split` on file contents) fails to compile out of the box (`cannot use content (variable of type []byte) as string value in argument to strings.Split`). The only working escape hatch found while building improvement #44's Task B was `(call string content)`, which happens to compile only because `call`'s codegen (zero.go:1167-1181) blindly emits `funcName(args)` for *any* symbol name, including Go builtins — an unvalidated, undocumented escape hatch rather than an intentional feature. A real fix should add an explicit `to_string`/`bytes_to_string` node (or make `read_file` itself return a decoded string) rather than relying on `call` accepting arbitrary Go identifiers.
* **Done (commit `709d80b`, 2026-07-23):** added `to_int`/`to_float` nodes (`strconv.Atoi`/`strconv.ParseFloat` inline, zero-value-on-error via the discarded-error Go idiom) and, per the related finding above, `to_string`/`bytes_to_string` nodes so `[]byte` from `read_file` converts cleanly without the undocumented `(call string ...)` escape hatch. Covered by `tests/test_primitives.zero`. Re-verified 2026-07-24 while grooming: `(let (n (to_int "42")) (print (+ n 1)))` transpiles, builds, and prints `43`. This note was missing even though the ranked table already correctly said "Done (2026-07-23)" — backfilled during the 2026-07-24 groom pass, same gap pattern as bug #22.

### 18. `if` condition rejects compound/logical expressions, and silently mis-compiles them
* **Description:** `generateStatement`'s `"if"` case parses its condition by hand: it requires `condNode` to have exactly 3 children, reads `condNode.Children[0].Value` as the operator against a hardcoded whitelist (`== != < > <= >=`, plus `=` aliased to `==`), and reads `condNode.Children[1].Value` / `condNode.Children[2].Value` directly as left/right operands. This ignores the separate, already-working generic expression generator a few hundred lines later (zero.go:1160-1172) that recursively handles `+ - * / < > and or ==` (missing `!= <= >=` too) via `generateStatement` recursion — value-position expressions like `(let (x (and (> a 1) (< a 10))) ...)` already compile correctly through that path. The `if` handler never delegates to it.
* **Why:** Discovered while regression-testing the bug #16 fix (single-branch `if`): tried `(if (and (> 5 1) (< 5 10)) ...)` as a natural follow-on guard-clause pattern and hit a hard error, then tried `(if (> (+ 2 3) 4) ...)` (a compound left operand) and got no error at all but corrupted Go output (`if  > 4 {`, empty left operand — `.Value` on a `List` node is empty string), which would only surface as a confusing Go compiler error far from the actual `.zero` source line. The silent-corruption case is strictly worse than bug #16's old hard error, since it produces plausible-looking but broken transpiler output.
* **Impact:** 8/10 (High — blocks compound boolean conditions and arithmetic-on-either-side comparisons, both extremely common in AI-generated guard clauses; the silent-corruption half is also a correctness/trust issue for the transpiler itself).
* **Repro (rejected case):** `(cli_app (if (and (> 5 1) (< 5 10)) (print "in range")))` → `{"reason":"unsupported operator in if cond: and",...}`.
* **Repro (silent-corruption case):** `(cli_app (if (> (+ 2 3) 4) (print "big")))` → transpiles with no error, but emits `if  > 4 {` in `server.go` (missing left operand), which then fails at `go build`/`go run` with a Go syntax error unrelated-looking to the original `.zero` line.
* **Fix sketch:** replace the `if` handler's manual condition parsing with a call into the existing generic expression generator (zero.go:1182-1196), extending that generator's operator whitelist to include `!=`, `<=`, `>=` (currently only `+ - * / < > and or ==`) so it covers every comparison `if` already claims to support. This both fixes the `and`/`or` rejection and the compound-operand corruption in one change, since the generic path already recurses via `generateStatement` instead of reading `.Value` off a `List` node.
* **Addendum (2026-07-23):** `while`'s condition handler (zero.go:948-974) is a near-verbatim copy of `if`'s and has the identical flaw: same hardcoded operator whitelist (no `and`/`or`), same `.Value`-off-a-`List` silent-corruption bug for a compound operand (e.g. `(while (> (+ i 1) 10) ...)` emits `for  > 10 {`). Found while grooming the backlog after fixing bug #14. Recommend widening this item's scope (and fix) to cover both `if` and `while` in one pass, since both would delegate to the same fixed condition-codegen helper — no separate bug filed to avoid double-counting effort against the same root cause.
* **Groomed (2026-07-23):** re-verified both halves against current zero.go — `if` handler now at 840-879 (op whitelist 852), `while` handler now at 947-973 (op whitelist 959); both repros (the `and`/`or` rejection and the `if  > 4 {`/`for  > 10 {` corruption) still reproduce verbatim. Description, impact, and fix sketch all still accurate; only line numbers drifted (+~20) from the bug #16 fix landing in between.
* **Done (2026-07-24):** fixed as an incidental side effect of improvement #53's IR refactor (landed the day after the groom note above, which is why that note was left saying "still reproduces" — it was accurate at the time it was written, just never revisited after the next day's unrelated change happened to fix it). `if`/`while` are now both handled by `emitGoIR`'s shared `"if"`/`"while"` cases, which render the condition via `generateExpression` (the generic recursive expression path) instead of the old hand-rolled flat `(op a b)` parser — exactly the fix sketch's suggested approach, arrived at via the IR unification rather than a targeted fix. Re-verified both repros directly: `(if (and (> 5 1) (< 5 10)) (print "in range"))` → transpiles and runs, prints `in range`; `(if (> (+ 2 3) 4) (print "big"))` → transpiles and runs, prints `big`; the `while` equivalent (`(while (and (< i 3) (> 10 i)) ...)`) also transpiles and runs correctly. This ranked-table row's "Done (2026-07-23)" status was already correct — it's the detail section's last note that was stale and self-contradicting; this note replaces it as current.

### 19. `//line` directive corrupts any compound expression nested inside another compound expression
* **Description:** `generateStatement` (zero.go:565-578) wraps `generateStatementRaw`'s output with a `//line %s:%d\n` prefix for a fixed list of statement heads (`return, res_json, res, let, do, try_let, spawn, if, print, db_connect, sql_query, append, map_set, map_delete, for, sleep, write_file, mkdir, exec, while, match, set, call, cli_args`). This is correct when the result is emitted as its own standalone line. But several code paths call `generateStatement` (not `generateStatementRaw`) to embed a *sub-expression* inline inside a larger `fmt.Sprintf` — e.g. the binary-operator case (zero.go:1160-1173, `arg1 := generateStatement(...)`), `assert_semantic`'s var/condition args, and `cli_args`'s index argument. Whenever the nested node's head happens to be one of the wrapped heads — most commonly `call` — the injected `//line ...\n` comment lands mid-expression and corrupts the surrounding Go statement (the rest of that source line becomes a comment, and the following text becomes a dangling, uncompiled fragment).
* **Why:** Discovered while fixing bug #13: the fix sketch's literal suggestion (`generateStatement(valNode, reqVar, depth+1)` for `return`'s compound value) produced exactly this corruption for `(return (call f x))`, even though it worked fine for `(return (+ a b))` since `+` isn't in the wrapped-heads list. Bug #13 itself was fixed by using `generateStatementRaw` instead, but the same latent defect remains at every other call site that recurses via `generateStatement` for a sub-expression position — e.g. `(+ (call f) 1)` reproduces the identical corruption today, independent of bug #13.
* **Impact:** 8/10 (High — silent, compiler-visible-only corruption; affects every arithmetic/logical operator and several other primitives whenever an argument is itself a `call` or another wrapped head).
* **Repro:** `(cli_app (defun one () (return 1)) (defun addit () (type_hint return "int") (return (+ (call one) 1))) (print (call addit)))` → transpiles with no error, but `server.go` contains `return (//line ...:5\n\t\tone() + 1)`, which fails `go build`.
* **Fix sketch:** introduce a distinct `generateExpression(node, reqVar, depth)` (a thin alias for `generateStatementRaw`, no `//line` wrapping) and audit every call site that currently uses `generateStatement` to embed a result *inside* a larger expression string rather than emit it as an independent statement line — switch those to `generateExpression`. Leave call sites that truly emit independent top-level statements (e.g. `do`'s per-child loop) on `generateStatement` so they keep their line-mapping.
* **Groomed (2026-07-23):** re-verified against current zero.go — wrapper now at 588-601, binary-op recursion site now at 1193-1194 (both drifted from the bug #16 fix). Mechanism, repro (`return (//line ...\n\t\tone() + 1)`), and fix sketch all still accurate.
* **Done (2026-07-24):** fixed essentially per the fix sketch, arrived at via improvement #53's IR refactor rather than a standalone targeted fix (same story as bug #18, landed the day after the groom note above): `zero.go` now has a distinct `generateExpression(node, reqVar, depth)` function — a thin alias for `generateStatementRaw` with no `//line` wrapping — and the binary-operator/IR emission paths (`emitGoIR`'s `"binop"`/`"if"`/`"while"` cases) call it for sub-expression positions instead of the wrapping `generateStatement`. Re-verified the repro directly (with a corrected type_hint on the inner `one` defun, which the original repro text was missing): `(defun one () (type_hint return "int") (return 1)) (defun addit () (type_hint return "int") (return (+ (call one) 1)))` now transpiles, builds, and prints `2` with no `//line` corruption. This ranked-table row's "Done (2026-07-23)" status was already correct — only the detail section's last note was stale; this note replaces it as current.

### 20. `call` arguments that are compound expressions are silently dropped to empty
* **Description:** The `"call"` case in `generateStatementRaw` (zero.go:1197-1211) builds its argument list with `if argNode.Type == "STRING" { quoted } else { args = append(args, argNode.Value) }` — it never recurses via `generateStatement`/`generateStatementRaw` the way `return`, `set`, `append`, `map_set`, and the binary-operator case all do. `.Value` is only populated for leaf tokens (SYMBOL/STRING/NUMBER); for a compound-expression argument such as a nested `(call ...)` or `(+ a b)`, `.Value` is empty, so that argument silently vanishes from the generated Go call — no error, no placeholder, just a shorter argument list than the source specified.
* **Why:** Found while regression-testing bug #14's fix: a test fixture using `(call strconv.Itoa (call rand.Intn 100))` transpiled with no error but produced `strconv.Itoa()` in `server.go` — zero arguments — which only surfaces as a confusing downstream Go compiler error (`not enough arguments in call to strconv.Itoa`) far from the actual cause. This is the same class of defect bug #13 fixed for `return`'s value position (bare `.Value` read instead of recursion), just in `call`'s argument-list position, which bug #13's fix didn't touch.
* **Impact:** 8/10 (High — `call` is the primary way to invoke any function, Go builtin, or package method in Zero; any nested/compound argument to any `call` is affected, and the failure is silent at the transpile stage).
* **Repro:** `(cli_app (defun add (a b) (type_hint a "int") (type_hint b "int") (type_hint return "int") (return (+ a b))) (print (call strconv.Itoa (call add 1 2))))` → transpiles with no error, but `server.go` contains `strconv.Itoa()` (the nested `(call add 1 2)` argument is gone entirely), which fails `go build` with `not enough arguments in call to strconv.Itoa`.
* **Fix sketch:** in the `call` argument loop, recurse into non-STRING args via `generateStatementRaw(argNode, reqVar, depth+1)` instead of reading `argNode.Value` directly (mirroring the bug #13 fix for `return`). Watch for the same `//line`-splicing hazard bug #19 documents if `generateStatement` (the wrapping variant) is used instead of `generateStatementRaw` — a nested `call` argument would trigger it, since `call` is itself one of the wrapped heads.
* **Groomed (2026-07-23):** re-verified against current zero.go — `call` case now at 1196-1210 (drifted from the bug #16 fix). Repro (`strconv.Itoa()` with the nested arg silently dropped) still reproduces verbatim.
* **Done (2026-07-24):** verified fixed — `(print (call strconv.Itoa (call add 1 2)))` now transpiles, builds, and prints `3` with the nested `(call add 1 2)` argument correctly passed through rather than dropped. Same story as bugs #18/#19: this ranked-table row's "Done (2026-07-23)" status was already correct; only the detail section's last groom note was stale, written the day before the fix actually landed (via improvement #53's IR refactor / the `generateExpression` split documented in bug #19's Done note). Not independently tracked which specific commit fixed `call`'s argument recursion specifically, since it wasn't isolated from the surrounding refactor — re-verify against `zero.go`'s current `call` case directly if the exact mechanism matters for future work.

### 21. `env` emits an unquoted Go identifier instead of a string literal
* **Description:** The `"env"` branch of the value-expression handler (zero.go:732-740) has two consecutive, unconditional assignments to `valStr`:
  ```go
  if keyNode.Type == "STRING" {
      valStr = fmt.Sprintf("os.Getenv(%q)", keyNode.Value)
  valStr = fmt.Sprintf("os.Getenv(%s)", keyNode.Value)
  }
  ```
  The second line (misindented, no `else`) always runs immediately after the first and unconditionally overwrites it, so the correctly-quoted result is dead code — every `(env "KEY")` call actually emits `os.Getenv(KEY)`, a reference to an undefined bare Go identifier `KEY`, instead of `os.Getenv("KEY")`.
* **Why:** Found while re-verifying `tests/test_middleware.zero` (previously dismissed in bug #12/#13/#14's notes as a "pre-existing, unrelated failure" — missing `TOKEN` env var) during this session's backlog grooming pass. The real cause isn't a missing env var at all: `go build` fails with `undefined: TOKEN` because the generated code is `token := os.Getenv(TOKEN)`, not `os.Getenv("TOKEN")`. `env` is documented in improvement #9 as the "Secure by Default" primitive for injecting credentials without hardcoding — this defect means it has never actually worked past the transpile stage.
* **Impact:** 8/10 (High — unconditionally breaks every use of a core, documented, security-critical primitive; zero workaround since the bug fires on every call, not an edge case).
* **Repro:** `(cli_app (let (t (env "TOKEN")) (print t)))` → transpiles with no error, but `server.go` contains `t := os.Getenv(TOKEN)`, which fails `go build` with `undefined: TOKEN`.
* **Fix sketch:** delete the dead second assignment line (`valStr = fmt.Sprintf("os.Getenv(%s)", keyNode.Value)`), keeping only the `%q`-quoted version inside the `if keyNode.Type == "STRING"` block.
* **Done (2026-07-23):** Deleted the dead second assignment line at zero.go:739 exactly per the fix sketch — a fully-specified one-line diff, so applied directly (no journal, no delegation) per the Working Protocol's triviality carve-out. Added `tests/test_env.zero` as a regression fixture; transpiled, built, and ran with `ZERO_TEST_TOKEN=expected-secret go run server.go`, confirming `os.Getenv("ZERO_TEST_TOKEN")` (quoted) is now emitted and the value round-trips correctly. Re-ran the full `tests/*.zero` suite through transpile + `go build`: this fix also unblocks `tests/test_middleware.zero`, which previously failed `go build` with `undefined: TOKEN` (misdiagnosed in bug #12/#13/#14's notes as a missing-env-var issue — the real cause was this bug) and now builds cleanly. Only remaining failure is the pre-existing, separately-tracked bug #22 (`tests/test.zero`'s unvendored `uuid` module).

### 22. `tests/test.zero` depends on an unvendored external module, breaking offline builds
* **Description:** `tests/test.zero` transpiles a `(import "github.com/google/uuid")` root-level import, but `go.mod` only declares `module zero` / `go 1.20` with no `require` entries, and there is no `go.sum` in the repo. Running `go build` on the generated `server.go` fails with `no required module provides package github.com/google/uuid; to add it: go get github.com/google/uuid` — which requires network access this sandboxed environment may not have.
* **Why:** This exact failure has been noted in passing as a "pre-existing, unrelated" failure in the verification notes for bugs #12, #13, and #14, but was never filed as its own tracked item — each session was rediscovering and re-dismissing the same known-broken fixture. Filed during this session's backlog grooming pass so it stops being silently reproduced and ignored.
* **Impact:** 3/10 (Low-Medium — doesn't affect transpiler correctness for the general case, but blocks a fully-green `tests/*.zero` suite and wastes verification time every session that re-discovers it).
* **Repro:** `go run zero.go tests/test.zero && go build .` → `server.go:16:2: no required module provides package github.com/google/uuid`.
* **Fix sketch:** either (a) run `go mod tidy`/`go get github.com/google/uuid` once with network access and commit the resulting `go.mod`/`go.sum` entries, or (b) swap the fixture's import to a stdlib-only package (e.g. drop the `uuid` dependency and generate an ID a different way) so the whole `tests/*.zero` suite builds offline with no network dependency at all.
* **Done (commit `aa2d2b0`, fix sketch option (b)):** the `(import "github.com/google/uuid")` line was simply unused dead weight in the fixture (never referenced by any route/struct in the file) — removed it entirely rather than vendoring the module, so `tests/test.zero` now builds offline with no network dependency. Verified 2026-07-24: `go.mod`/`go.sum` have no `uuid` entry and `grep -rn uuid tests/ examples/` finds nothing anywhere in the repo. This note was missing even though the ranked table already correctly said "Done (2026-07-23)" — backfilled during the 2026-07-24 groom pass so the detail section isn't silently out of sync with the table.
* **Related, not a new bug (2026-07-24):** `tests/test_schema.zero` (added by improvement #53's era of work) genuinely uses `github.com/mattn/go-sqlite3`, which **is** correctly declared in `go.mod`'s `require` — but `go.sum` is deliberately `.gitignore`d (see the "Go modules" section of `.gitignore`), so a fresh clone has no `go.sum` and `go build` on that fixture's generated `server.go` fails with `missing go.sum entry` until someone locally runs `go mod tidy` (confirmed working with network access this session, same as this bug's original fix). This isn't a transpiler defect — it's a consequence of the project's own choice not to track `go.sum` — so no bug filed; noted here since it surfaces with the exact same symptom shape as this bug and was flagged as a fresh-looking failure in the #53 IR journal and again in this session's #49 journal before being traced back to "just run `go mod tidy`."

### 23. Lexer has no comment syntax — `;`-prefixed comments in documented examples fail to lex
* **Description:** `Lexer.NextToken()`'s leading whitespace-skip loop (zero.go:81-91) only special-cases `unicode.IsSpace`; there is no handling anywhere for `;` (the conventional Lisp/Scheme line-comment marker). Any `;` character falls through to the final `reportError(fmt.Sprintf("Unexpected character: %c", ch), ...)` at zero.go:148, so a `.zero` file containing a `;;`-style comment fails to lex at all, before the parser is ever reached.
* **Why:** Found while auditing README.md/docs/index.html for documentation-vs-shipped-feature coverage during the 2026-07-23 backlog groom. Two of README.md's own code examples ("AI Orchestration Example" and "Implicit Context Threading") used `;;`-prefixed inline comments (a natural habit for any LLM that has seen Lisp/Scheme training data) and neither actually transpiled — confirmed via `go run zero.go` against both snippets verbatim. Comment support is a near-universal expectation for a Lisp-like language and something LLM-generated `.zero` code will very likely reach for unprompted.
* **Impact:** 7/10 (High — silently breaks any AI-generated `.zero` file that includes a comment, a extremely common and low-effort habit; also directly broke two of the project's own flagship README examples as documented).
* **Repro:** `echo '(cli_app ;; comment\n  (print "hi"))' > /tmp/t.zero && ./zero /tmp/t.zero` → `{"reason":"Unexpected character: ;","line":1,...}` (any `;` anywhere triggers it, not just leading-column comments).
* **Fix sketch:** in the leading-whitespace-skip loop (zero.go:81-91), add a branch that recognizes `;` and consumes characters up to (but not including) the next `\n` or EOF, the same way `unicode.IsSpace` characters are silently skipped today — no new token type needed since comments carry no semantic value.
* **Doc mitigation applied (2026-07-23, superseded same day):** since implementing lexer comment support was initially out of scope for the groom pass that found this bug (no backlog-item implementation during grooming), the two broken README.md examples were first fixed by removing the non-functional `;;` comments and moving their explanatory content into the surrounding prose instead. Once this bug shipped (below), real `;;` comments were restored to both examples so the docs actually demonstrate the fixed capability rather than working around its absence.
* **Done (2026-07-23):** implemented directly (trivial, fully-specified fix, no delegation/journal needed) exactly per the fix sketch — `Lexer.NextToken()`'s leading-whitespace-skip loop (zero.go:80-95) now special-cases `;`, consuming characters up to (but not including) the next `\n` or EOF, mirroring how `unicode.IsSpace` is skipped. Verified: (1) the original repro (`;;` comment inside a `cli_app` block) now lexes and transpiles cleanly; (2) `;` inside a string literal (`"a;b"`) is unaffected, since string tokenization short-circuits before the comment branch; (3) a comment with no trailing newline at EOF is consumed correctly; (4) full regression pass of every file in `tests/*.zero` through `./zero` + `go build -o <scratch> .` shows no new failures — the only build failure (`test.zero`, missing `github.com/google/uuid`) is the pre-existing, separately tracked bug #22, unrelated to this change. (5) Restored real `;;` comments into both of README.md's previously-mitigated examples ("AI Orchestration Example", "Implicit Context Threading") and re-verified both transpile and `go build` cleanly with the comments in place.

### 24. No way to write a `defun` with no return value (void function)
* **Description:** `defun`'s codegen (zero.go:389-406) always defaults `returnType` to `"string"` unless a `(type_hint return "...")` is given (zero.go:399-402), and always emits that return type in the generated Go function signature (zero.go:406, `func %s(%s) %s {`). There is no way to omit the return type entirely: a `defun` with no `type_hint return` and a body that never executes `return` (e.g. a pure side-effect helper that only `print`s) generates `func name(args) string { ...no return... }`, which fails `go build` with `missing return`. Attempting the natural escape hatch, `(type_hint return "void")`, does not work either — `"void"` is not a valid Go type, so it instead fails with `undefined: void`. There is no supported way to write a `defun` whose only job is a side effect.
* **Why:** Found via the same README.md audit that surfaced bug #23: the "Implicit Context Threading" example's `fetch_user` `defun` (a `print`-only helper, the natural minimal example of a side-effecting function) fails `go build` with `missing return` even after bug #23's comment issue is fixed, independent of and in addition to it. Side-effect-only functions (logging, printing, writing to a channel, mutating shared state) are one of the most common function shapes in real code, so this is a broad, everyday gap, not an edge case.
* **Impact:** 8/10 (High — blocks an extremely common, basic function shape with zero documented workaround; every `defun` is currently forced to either fabricate a dummy string return value or hit a hard Go compile error).
* **Repro:** `(cli_app (defun greet (name) (print "Hello" name)) (call greet "World"))` → transpiles with no error, but `go build` fails with `missing return`. Adding `(type_hint return "void")` instead fails with `undefined: void`.
* **Fix sketch:** special-case the string `"void"` (or introduce a dedicated `(type_hint return "none")` convention) in the `type_hint` handling around zero.go:399-402 so `returnType` becomes the empty string rather than the literal text `"void"`; then adjust the `Sprintf` at zero.go:406 to avoid the resulting double-space when `returnType == ""` (`func %s(%s) {` instead of `func %s(%s)  {`). Go itself requires no `return` statement for a function with no declared return type, so this alone should resolve the `missing return` error without any change to `bodyCode` generation.
* **Done (2026-07-23):** Implemented the `void` special case in `returnType`. Delegated to `agy` via `gemini-3.1-pro-low`. Verified by running `go build` and `go run` successfully against a side-effect test using `(type_hint return "void")`.

### 25. `let` with more than one body statement infinite-recurses instead of erroring
* **Description:** Both `generateStatementRaw` (Go) and `generateJSStatementRaw` (JS) validate a `let` node's arity with `if len(node.Children) < 3 { reportError(...) }` — a lower bound only. The chain-consuming loop right after it requires *exactly* 3 children (`len(curr.Children) == 3`) to treat `curr` as another link in a nested-`let` chain. If the original node has *more* than 3 children (e.g. `(let (var val) stmt1 stmt2)` — a natural mistake, since nothing in the grammar signals `let`'s body must be a single form, unlike `do`, which is variadic), the `< 3` check passes but the loop's `== 3` condition fails on the very first iteration, so the loop body never runs and `curr` is never advanced past the original node. `bodyCode := generateStatement(curr, reqVar, depth+1)` then recurses on the *unchanged* `curr` — i.e. the exact same `let` node — calling straight back into the same code path with an incremented depth, forever, until the depth-1000 safety guard added for bug #5 fires and reports `"AST too deep"` instead of a validation error naming the actual problem.
* **Why:** Found while writing a README.md example for the JS/`web_app` backend (previously entirely undocumented) during a 2026-07-24 documentation pass — a natural first attempt at `(let (count 0) (set count 1) (print count))` (three body-shaped forms after the binding) hit this instead of a normal AI self-correction loop error. Reproduces identically in the Go backend with no JS/`web_app` involvement at all (`(cli_app (let (count 0) (set count 1) (print count)))`), so it's not a JS-backend-specific issue — both backends share the same latent arity gap. Deep, misleading errors like `"AST too deep"` are worse than a normal validation error for the AI self-correction loop this transpiler exists to support (see README's "Why Zero?"): the error text gives no hint that the fix is "wrap multiple body statements in `(do ...)`."
* **Impact:** 6/10 (Medium-High — a very natural authoring mistake produces a confusing, unrelated-looking error with no actionable hint, defeating the transpiler's core self-correction-loop value proposition for this specific case).
* **Repro:** `(cli_app (let (count 0) (set count 1) (print count)))` → `{"reason":"AST too deep: exceeded maximum nesting limit of 1000","line":1,"column":10}` on both the Go and JS (`web_app`) backends, pre-fix.
* **Fix sketch:** tighten both backends' entry check from `len(node.Children) < 3` to `len(node.Children) != 3`, with a clearer message pointing at `do` for multiple body statements.
* **Done (2026-07-24):** Applied the fix-sketch diff to both `generateStatementRaw` and `generateJSStatementRaw` — entry check is now `!= 3` with the message `"let expects (let (var val) body) — wrap multiple body statements in (do ...)"`. Verified: the repro above now produces that clear error instead of the depth-guard message; added `tests/test_let_arity.zero` (a correctly `do`-wrapped multi-statement `let`) to the regression suite; re-ran the full byte-diff of every `tests/*.zero`/`examples/*.zero` fixture's generated output against the pre-fix binary — zero output changes for any valid (non-malformed) program, confirming this only changes behavior for previously-infinite-recursing malformed input. `go build`/`go vet`/`go test ./...` all pass.

### 26. `list`/`dict` literals only work as a `let` binding's direct value, everywhere else fails with a generic error
* **Description:** `list`/`dict` construction (`(list "a" "b")`, `(dict ("k" "v"))`) has no general `case "list"`/`case "dict"` anywhere in `generateStatementRaw` (Go) or `generateJSStatementRaw` (JS) — the only place either node kind is handled is inside `let`'s hand-rolled value-position special case (Go: zero.go ~1237-1263; JS: ~1889-1917, both checking `funcName == "list"`/`funcName == "dict"` on the *value* of a `(let (var val) body)` binding). Any attempt to construct a list/dict literal anywhere else — a `return` value, a `print`/`call` argument, nested inside another `list`/`dict`, etc. — falls through to the generic `"Unknown statement: list"` / `"Unknown statement: dict"` error at the end of `generateStatementRaw`, with no hint that the real constraint is "must be a `let`'s direct value."
* **Why:** Found while implementing improvement #60 (`map_get`/`list_get`) and probing how far list/dict expression support extends, to make sure the new read primitives' fixtures exercised realistic usage. The restriction is invisible in the language reference (README/skill docs show `list`/`dict` only ever used inside `let` examples, so it reads as a stylistic choice rather than a hard limitation) and is inconsistent with the Phase 1 interpreter (`-run`, improvement #49), whose `evalList` handles `list`/`dict` as ordinary expressions with no such restriction at all.
* **Impact:** 7/10 (High — blocks a very natural pattern: building and immediately returning/passing/printing a collection literal without first binding it to a throwaway variable; also a cross-backend inconsistency between the interpreter and both text-codegen backends).
* **Repro:**
  - `(cli_app (print (list "a" "b")))` → `{"reason":"Unknown statement: list","line":1,"column":17}`
  - `(cli_app (defun make () (return (list "a" "b"))) (print (call make)))` → `{"reason":"Unknown statement: list","line":1,"column":33}`
  - Contrast: `(cli_app (let (l (list "a" "b")) (print l)))` transpiles and runs fine.
  - Contrast: `./zero -run` on the first repro runs correctly under the interpreter, printing `[a b]`.
* **Fix sketch:** extract the `list`/`dict` construction logic already duplicated inside both backends' `let`-value special-casing into standalone per-backend helper functions (e.g. `generateListLiteral`/`generateDictLiteral` and JS equivalents), then add general `case "list"`/`case "dict"` dispatch arms to `generateStatementRaw`/`generateJSStatementRaw` that call the same helpers — so a list/dict literal works in any expression position, not just as a `let`'s direct value. Keep the existing `let`-binding call site working unchanged (call the extracted helper from there too) to avoid regressing #8/#31/#60's existing fixtures. Both backends currently only support string-typed elements/values (`[]string`/`map[string]string` in Go, plain arrays/objects in JS) — preserve that scope rather than expanding element typing as part of this fix.
* **Done (2026-07-24):** Implemented as new shared-IR kinds (`lowerShared`/`emitGoIR`/`emitJSIR`, the same infrastructure improvement #60's `map_get`/`list_get` uses) rather than the fix sketch's "extract a per-backend helper" approach — `lowerShared` is checked at the very top of both `generateStatementRaw` and `generateJSStatementRaw`, before either backend's hand-rolled `let`-value special case is ever reached, so `list`/`dict` now construct correctly in any expression position (`print` argument, `return` value, nested inside `do`, etc.) via one small addition per emitter, with zero changes to the existing, already-working `let`-value code path — lower risk than extracting/rewiring a shared helper into both places. Go emits `[]string{...}`/`map[string]string{...}` (element/value handling identical to the pre-existing `let`-value logic: quoted for `STRING` children, raw `.Value` otherwise — scope intentionally unchanged, no compound-expression elements). JS emits `[...]`/`{...}}`, using `generateJSExpression` for non-`STRING` elements/values to match the JS `let`-case's own existing convention. New regression fixture `tests/test_list_dict_expr_position.zero` covers `list`/`dict` as a `print` argument, a `defun` return value (with explicit `type_hint return "[]string"`/`"map[string]string"`, since a `defun`'s return type still defaults to `string` — unrelated pre-existing behavior, not part of this bug), and confirms the original `let`-value usage is byte-identical. Verified via the Go backend (`go build` + run), the JS/`web_app` backend (`node`), and the interpreter (`-run`) — all three produce matching output. Full `tests/*.zero`/`examples/*.zero` suite byte-diffed against a pre-change baseline binary (`git stash` + rebuild): zero output differences for any existing fixture, confirming the change is purely additive. `go build -o <scratch> zero.go` and `go vet zero.go` both pass. No benchmark re-run needed (doesn't touch `defun`/`type_hint`/`read_file`/`str_split`/`test`, per the Working Protocol's exception for provably-unaffected changes). Delegation unavailable this session (agy weekly quota exhausted, Ollama not installed) — implemented directly per Working Protocol point 10.

### 27. Go backend's `list`/`dict` literal construction silently drops compound-expression elements/values to empty
* **Description:** Both the Go backend's `let`-value special case (unchanged by bug #26's fix) and bug #26's new general-position `emitGoIR` `"list"`/`"dict"` cases build each element/value with `if kid.Type == "STRING" { quoted } else { kid.Value }` — never recursing via `generateStatement`/`generateStatementRaw` the way `return` (post bug #13), `call` args (post bug #20), `append`, and `map_set` all do. `.Value` is only populated for leaf tokens (SYMBOL/STRING/NUMBER); for a compound-expression element such as a nested `(call ...)`, `.Value` is empty, so that element silently becomes an empty string in the emitted `[]string{...}`/`map[string]string{...}` literal — no error, just a malformed Go literal (a stray leading/embedded comma) that fails at `go build`, far from the actual `.zero` source line.
* **Why:** Found while regression-testing bug #26's fix: probed whether a `list`/`dict` element could itself be a function call (`(list (call one) "b")`), a natural pattern once list/dict literals work in any expression position. Confirmed this is a pre-existing defect, not introduced by bug #26 — it reproduces identically against the pre-#26 binary via the original `let`-value path (`(let (l (list (call one) "b")) ...)`). The JS backend and the Phase 1 interpreter are both unaffected: JS's equivalent `let`-value case already recurses via `generateJSExpression` for non-`STRING` elements/values (and bug #26's new JS `"list"`/`"dict"` cases were written to match that), and the interpreter fully evaluates every child node regardless of position — so this is a Go-backend-only gap.
* **Impact:** 6/10 (Medium-High — silent, compiler-visible-only corruption of a common pattern: building a list/dict from computed values rather than only literals; same failure shape as bugs #13/#20 but scoped to list/dict literal positions specifically).
* **Repro:** `(cli_app (defun one () (type_hint return "string") (return "hi")) (print (list (call one) "b")))` → transpiles with no error, but `server.go` contains `fmt.Println([]string{, "b"})` (note the leading comma — the first element vanished), which fails `go build` with `syntax error: unexpected comma, expected expression`. The `dict`-value equivalent (`(dict ("k" (call one)))`) fails the same way with `unexpected }, expected expression`. Reproduces identically via the pre-existing `let`-value path (e.g. `(let (l (list (call one) "b")) (print l))`), confirming it predates bug #26's fix.
* **Fix sketch:** in both the `emitGoIR` `"list"`/`"dict"` cases (added by bug #26) and the untouched `let`-value special case's `list`/`dict` branches, replace the `else { kid.Value }` / `.Value` reads with a recursive call — `generateStatement`/`generateStatementRaw` for the non-`STRING` branch, mirroring the bug #13/#20 fix pattern exactly. Watch for the bug #19 `//line`-splicing hazard: use the non-wrapping `generateExpression`/`generateStatementRaw` variant, not the wrapping `generateStatement`, if the recursive call could embed a wrapped-head node (e.g. a nested `call`) inline inside the `[]string{...}`/`map[string]string{...}` literal text.
* **Done (2026-07-24):** Implemented exactly per the fix sketch in all four call sites (`emitGoIR`'s `"list"`/`"dict"` cases at zero.go:1060-1086, and the `let`-value special case's `list`/`dict` branches at zero.go:1329-1355): the non-`STRING` element/value branch now calls `generateExpression(kid, reqVar, depth+1)` (the existing non-wrapping alias documented in bug #19's Done note) instead of reading `.Value` directly — avoiding the `//line`-splicing hazard the fix sketch flagged, since a nested `call` argument would otherwise trip it. Dict keys were left untouched (out of this bug's stated scope of "elements/values") — that gap is real and reproducible (confirmed while verifying this fix) and is filed separately as bug #28 rather than folded in here, since expanding scope mid-fix would have made this change harder to verify in isolation. No delegation available this session (agy weekly quota exhausted per this session's explicit instruction, Ollama not running) — implemented directly per Working Protocol point 10. New regression fixture `tests/test_list_dict_compound.zero` covers all four affected positions (list/dict, general-expression-position/`let`-value-position) with a nested `(call one)` element/value; transpiled and `go build`-verified, all four positions now correctly emit `one()` instead of a silently-dropped empty string. Full `tests/*.zero`/`examples/*.zero` suite byte-diffed against a pre-fix baseline binary (`git stash` + rebuild): zero output differences for any existing fixture — only the new fixture's own output differs, confirming the change is purely additive with no regressions. `go build`/`go vet` both pass. No benchmark re-run needed (doesn't touch `defun`/`type_hint`/`read_file`/`str_split`/`test`, per the Working Protocol's exception for provably-unaffected changes).

### 28. `dict` literal keys silently drop to empty when the key is a compound expression, producing invalid Go syntax
* **Description:** Both the `emitGoIR` `"dict"` case and the `let`-value special case's `dict` branch read a dict entry's key via `kid.Children[0].Value` / `pair.Children[0].Value` unconditionally — quoted with `%q` if the key node is a `STRING`, otherwise used raw. `.Value` is only populated for leaf tokens (SYMBOL/STRING/NUMBER); for a compound-expression key such as `(call k)`, `.Value` is empty, so the key silently vanishes from the emitted `map[string]string{...}` literal, leaving a bare `:` before the value (e.g. `map[string]string{: "v"}`) — invalid Go syntax that fails `go build` far from the actual `.zero` source line.
* **Why:** Found while verifying bug #27's fix (which deliberately scoped itself to elements/values, not keys, per that bug's own fix sketch) — probed the natural next question, "does the same gap exist for keys," and confirmed it does. Same root-cause pattern as bugs #13/#20/#27 (leaf `.Value` read instead of recursing), just in the one remaining position (`dict` keys) none of those three touched.
* **Impact:** 6/10 (Medium — silent compile-time corruption, same class as #27, but keys are far more often literal strings/symbols than compound expressions in practice, making this a narrower real-world hit rate than #27's elements/values case).
* **Repro:** `(cli_app (defun k () (type_hint return "string") (return "id")) (print (dict ((call k) "v"))))` → transpiles with no error, but `server.go` contains `fmt.Println(map[string]string{: "v"})` (empty key before the colon), which fails `go build` with `syntax error: unexpected :, expected expression`.
* **Fix sketch:** mirror bug #27's fix exactly, but for the key position: in both call sites, replace the key's raw `.Value` read with `generateExpression(keyNode, reqVar, depth+1)` for the non-`STRING` branch (same non-wrapping alias, same `//line`-splicing hazard to avoid per bug #19).
